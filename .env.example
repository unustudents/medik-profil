# ============================================================
# Environment Variables — Medika Astro
# ============================================================
#
# Jangan edit file ini langsung.
# Salin ke .env.development atau .env.production, lalu isi sesuai environment.
#
# Cara pakai:
#   cp .env.development .env   → untuk development
#   cp .env.production .env    → untuk production
#
# Docker Compose membaca .env otomatis.
# ============================================================

# Strapi CMS URL (tanpa trailing slash)
STRAPI_URL=http://localhost:1337

# Strapi API Token (dari Strapi admin → Settings → API Tokens)
STRAPI_TOKEN=

# Site URL (untuk sitemap & canonical URLs)
SITE_URL=http://localhost:4321
